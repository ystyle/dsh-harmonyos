diff --git a/lib/index.js b/lib/index.js
index 4d3aa5b..7d95fdf 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -109,7 +109,7 @@ var SandboxPolicyService = class extends Service {
 	workspaceRoot;
 	constructor(ctx, config) {
 		super(ctx, "sandboxPolicy");
-		this.defaultMode = config.mode;
+		this.defaultMode = process.env.DSH_OHOS_FORCE_DANGER === '1' ? 'danger-full-access' : config.mode; // dsh-harmonyos: OHOS 无 OS 沙箱后端
 		this.workspaceRoot = resolveWorkspaceRoot(config.workspaceRoot ?? process.cwd());
 		ctx.sessionProjections.register({
 			key: "sandboxMode",
